TWISSJAVA: Twitter on Cassandra in Java.

Prerequisites:
 + Maven: this will load our build deps


Setup:
 + Install Cassandra 0.7 from http://cassandra.apache.org/ 
 + check out twissjava
   TODO MAKE THIS SUCK LESS:
 * copy twissjava/cassandra.yaml over cassandra's cassandra.yaml
 * run cassandra's bin/schematool localhost 8080 import
   

Run:
 + Start bin/cassandra
 + run `mvn jetty:run`
   + maven will download a ton of stuff the first run.  this is normal.
 + browse to http://localhost:8888
